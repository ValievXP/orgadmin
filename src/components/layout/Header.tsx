"use client";
import React from "react";

export function Header() {
  return (
    <header className="h-16 bg-[var(--bg-surface)] border-b border-[var(--border-default)] w-full shrink-0 z-10 sticky top-0">
      {/* Пустая зона навигации, как вы и просили. Позже сюда можно будет добавлять элементы */}
    </header>
  );
}
